# 版本 1.2 工作项

## 本次范围

- 修正 Plugin 节点默认输入语义
- 收紧自动生成 Flow 时对插件输入模板的约束
- 完善 Agent 流式返回和前端状态展示
- 完成 Flow 和 Agent 的结构化导出
- 完成 Agent 对话保存
- 补齐关键主路径测试

## 本次原则

- 新功能设计先评估是否增加用户使用成本
- 默认路径优先，复杂度优先由系统承担
- 导出结果既要便于用户复用，也要便于模型理解
- 测试优先覆盖真实流程，而不是字段穷举

## 准备修改

- 增加 Flow 和 Agent 的结构化导出接口
- 调整前端导出入口，直接下载结构化清单
- 增加 Agent 对话存储和读取接口
- 在 Agent 编辑页和测试对话中接入历史对话恢复
- 为导出和对话保存补充测试

## 已完成

- 版本 1.2 范围已记录

## 待完成

- Flow 结构化导出
- Agent 结构化导出
- Agent 对话持久化
- Agent 流式返回打断与保存收口
- 关键场景测试补充
